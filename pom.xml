<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://maven.apache.org/POM/4.0.0"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <description>Enterprise Lease Contract Management (ELCM) DTO Entity</description>
  <groupId>com.dodaso.ecosystem.elcm.data.model</groupId>
  <artifactId>elcm-data-model</artifactId>
  <modelVersion>4.0.0</modelVersion>
  <version>${revision}</version>
  <packaging>jar</packaging>

  <parent>
    <artifactId>spring-boot-starter-parent</artifactId>
    <groupId>org.springframework.boot</groupId>
    <relativePath/>
    <version>3.4.2</version>
  </parent>

  <properties>
    <build.number>1.0.0-SNAPSHOT</build.number>
    <java.version>21</java.version>
    <joinfaces.version>5.5.15</joinfaces.version>
    <!-- Explicit override: joinfaces-dependencies' own managed PrimeFaces
         version can lag behind PrimeFaces' actual releases. Declaring this
         property here (before/alongside the joinfaces-dependencies BOM
         import below) takes precedence over the BOM's own value, per
         Maven's property resolution and JoinFaces' documented override
         mechanism - this is what actually pins PrimeFaces to 15.x rather
         than whatever joinfaces 5.5.15 happens to default to. Confirmed via
         Maven Central as a real, published version as of this revision;
         bump to a newer 15.0.x patch as needed.
         Verify with: mvn dependency:tree | grep primefaces -->
    <primefaces.version>15.0.15</primefaces.version>
    <revision>1.0.0-SNAPSHOT</revision>
  </properties>

  <profiles>
    <profile>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <id>local</id>
      <properties>
        <activatedProperties>local</activatedProperties>
      </properties>
    </profile>
    <profile>
      <id>adev</id>
      <properties>
        <activatedProperties>adev</activatedProperties>
      </properties>
    </profile>
    <profile>
      <id>bdev</id>
      <properties>
        <activatedProperties>bdev</activatedProperties>
      </properties>
    </profile>
    <profile>
      <id>dit</id>
      <properties>
        <activatedProperties>dit</activatedProperties>
      </properties>
    </profile>
    <profile>
      <id>sit</id>
      <properties>
        <activatedProperties>sit</activatedProperties>
      </properties>
    </profile>
    <profile>
      <id>uat</id>
      <properties>
        <activatedProperties>uat</activatedProperties>
      </properties>
    </profile>
    <profile>
      <id>prod</id>
      <properties>
        <activatedProperties>prod</activatedProperties>
      </properties>
    </profile>
  </profiles>

  <build>
    <plugins>
      <!-- <plugin>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <groupId>org.springframework.boot</groupId>
        <executions>
          <execution>
            <id>repackage</id>
            <goals>
              <goal>repackage</goal>
            </goals>
            <configuration>
              <skip>true</skip>
            </configuration>
          </execution>
        </executions>
      </plugin> -->
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <groupId>org.apache.maven.plugins</groupId>
        <configuration>
          <source>21</source>
          <target>21</target>
        </configuration>
      </plugin>
      <!-- <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <groupId>org.apache.maven.plugins</groupId>
        <configuration>
          <skip>false</skip>
        </configuration>
        <executions>
          <execution>
            <id>default-war</id>
            <phase>none</phase>
          </execution>
          <execution>
            <id>war-exploded</id>
            <phase>package</phase>
            <goals>
              <goal>exploded</goal>
            </goals>
          </execution>
        </executions>
      </plugin> -->
    </plugins>
    <resources>
      <resource>
        <directory>${basedir}/src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
  </build>

  <dependencies>
    <dependency>
      <artifactId>spring-boot-starter-web</artifactId>
      <groupId>org.springframework.boot</groupId>
    </dependency>
    <dependency>
      <artifactId>jsf-spring-boot-starter</artifactId>
      <groupId>org.joinfaces</groupId>
      <version>${joinfaces.version}</version>
    </dependency>
    <dependency>
      <artifactId>primefaces-spring-boot-starter</artifactId>
      <groupId>org.joinfaces</groupId>
      <version>${joinfaces.version}</version>
    </dependency>
    <!-- ADDED 2026-08-19: entity/repository layer against dodaso-platform.dbs's elcm schema -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <artifactId>spring-boot-starter-test</artifactId>
      <groupId>org.springframework.boot</groupId>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>junit-vintage-engine</artifactId>
          <groupId>org.junit.vintage</groupId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <artifactId>baseline-common</artifactId>
      <groupId>com.dodaso.ecosystem.baseline.common</groupId>
      <version>1.0.0-SNAPSHOT</version>
    </dependency>

    <dependency>
      <artifactId>iams-data-model</artifactId>
      <groupId>com.dodaso.ecosystem.iams.data.model</groupId>
      <version>1.0.0-SNAPSHOT</version>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <artifactId>joinfaces-dependencies</artifactId>
        <groupId>org.joinfaces</groupId>
        <scope>import</scope>
        <type>pom</type>
        <version>${joinfaces.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

</project>
